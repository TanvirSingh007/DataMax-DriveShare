import ApiDefinition from "./api-definition";

export default class ApiMethods{
    
    API = new ApiDefinition()
    
    public uploadFile(name:string, description:string, folder:string, share:string, file:File){
        
    }
}